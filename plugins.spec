# -*- mode: python -*-

block_cipher = None


a = Analysis(['Files\\Python35\\Lib\\site-packages\\PyQt5\\plugins', 'PQ_Matp03.py'],
             pathex=['D:\\Program', 'E:\\MyCodes\\PythonCode\\HydrologySoftware_Code'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='plugins',
          debug=False,
          strip=False,
          upx=True,
          console=False )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               name='plugins')
